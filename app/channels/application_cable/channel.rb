# frozen_string_literal: true

module ApplicationCable
  # This Channel class is a subclass of ActionCable::Channel::Base and provides support
  # for the websocket protocol for communication between the server and the client.
  class Channel < ActionCable::Channel::Base
  end
end
